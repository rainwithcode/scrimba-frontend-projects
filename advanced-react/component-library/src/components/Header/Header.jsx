import "./header.css";

export default function Header() {
  return (
    <header className="header container">
      <h1 className="site-title">Component Library++</h1>
      <p className="site-description">
        Reusable, stylish UI components including badges, banners, cards, and
        testimonials with clean design and easy integration.
      </p>
    </header>
  );
}
